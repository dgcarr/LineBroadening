# LineBroadening

## Thesis extraction quickstart

Run the reproducible extraction in one command:

```bash
make thesis_extract
```

Validate expected topic/equation markers:

```bash
make thesis_verify
```

See `docs/thesis_extraction.md` for the offline OCR fallback pipeline.
